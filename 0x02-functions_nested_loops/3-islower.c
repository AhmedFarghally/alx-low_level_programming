#include "main.h"
#include <stdio.h>
/**
 * _islower - Returns 1 if c is lowercase, Returns 0 otherwise
 * islower - check the char
 * @c: input
 * Return: 1 or 0
 */

int _islower(int c)
{
	int r;
	/**/
	r = islower(c);
	return (r);
}
